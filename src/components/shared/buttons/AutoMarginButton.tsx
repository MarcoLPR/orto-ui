import { Button } from "baseui/button";
import { styled } from "baseui/styles/styled";

const AutoMarginButton = styled(Button, {
  margin: "0px auto !important",
});

export default AutoMarginButton;